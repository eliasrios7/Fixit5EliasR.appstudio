/*
 let monthEntered = prompt("Enter the full name of a month, capitalized.")
  
 if (monthEntered === 'December' || monthEntered === 'January' || monthEntered === 'February') 
        alert("That month is in the Winter.")
        /*
  } else if (monthEntered === 'March' || monthEntered === 'April' || monthEntered === 'May') {
      alert("That month is in the Spring.")
  } else if (monthEntered === 'June' || monthEntered === 'July' || monthEntered === 'August') {
      alert("That month is in the Summer.")
  } else if (monthEntered === 'September' || monthEntered === 'October' monthEntered === 'November') {
      alert("That month is in the Summer.")
  } else { 
      alert(`${monthEntered} is not a valid month. Have a great day.`)}
 */
